import { StoreModule } from "../../types/store/store";
import { BubbleChartOptions } from "../../types/options";
export declare function showCircleLegend(options: BubbleChartOptions): boolean;
declare const legend: StoreModule;
export default legend;
